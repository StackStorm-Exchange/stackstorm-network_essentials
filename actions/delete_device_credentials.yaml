---
name: delete_device_credentials
pack: network_essentials
runner_type: python-script
description: This deletes the device credentials from st2 store.
enabled: true
entry_point: delete_device_credentials.py
parameters:
  mgmt_ip:
    type: string
    description: The management IP address of the target device.
                 If the value is USER.DEFAULT it will remove the
                 common defaults provided by the user.
    required: true
    position: 0
