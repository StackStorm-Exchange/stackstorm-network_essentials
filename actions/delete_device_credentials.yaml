---
name: delete_device_credentials
pack: network_essentials
runner_type: run-python
description: Delete device credentials from st2 store
enabled: true
entry_point: delete_device_credentials.py
parameters:
  mgmt_ip:
    type: string
    description: Management IP address of the target device.
                 If the value is USER.DEFAULT it will remove
                 common defaults provided by user.
    required: true
    position: 0
